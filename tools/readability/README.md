# Readability metrics CLI (pinned release)

[adaptive-enforcement-lab/readability](https://github.com/adaptive-enforcement-lab/readability) is downloaded on first use by `readability.sh`.

Do not commit the `readability` binary; run via `scripts/report-readability-docs.sh`.
