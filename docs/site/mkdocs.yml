# Hydra user manual — static site (https://hydra-gitops.org/)
# Build from this directory: ./build.sh

site_name: Hydra User Manual
# Local default only. Publish pipeline overrides this via docs/site/build.sh --site-url.
site_url: http://127.0.0.1:8000/
site_description: Hydra GitOps CLI user manual
repo_url: https://github.com/hydra-gitops/hydra
repo_name: hydra-gitops/hydra
edit_uri: edit/main/hydra/docs/manual/

docs_dir: ../manual
site_dir: site

# Reload dev server when casts or site assets change (paths relative to this file).
watch:
  - ../manual
  - assets
  - stylesheets
  - javascripts
  - hydra_mkdocs

theme:
  name: material
  language: en
  logo: assets/hydra-logo-borderless.svg
  palette:
    - scheme: default
      primary: custom
      accent: indigo
      toggle:
        icon: material/brightness-7
        name: Switch to dark mode
    - scheme: slate
      primary: custom
      accent: indigo
      toggle:
        icon: material/brightness-4
        name: Switch to light mode
  features:
    - content.code.copy
    - navigation.instant
    - navigation.sections
    - navigation.top
    - search.suggest
    - search.highlight
    - toc.follow

plugins:
  - search
  - hydra_manual:
      asciinema_source: ../manual
      asciinema_site_path: asciinema

markdown_extensions:
  - admonition
  - attr_list
  - md_in_html
  - tables
  - toc:
      permalink: true
  - pymdownx.details
  - pymdownx.highlight:
      anchor_linenums: true
  - pymdownx.inlinehilite
  - pymdownx.tabbed:
      alternate_style: true
  - pymdownx.snippets
  - pymdownx.superfences:
      custom_fences:
        - name: mermaid
          class: mermaid
          format: !!python/name:pymdownx.superfences.fence_code_format

extra_css:
  - assets/asciinema-player/asciinema-player.css
  - stylesheets/extra.css

extra_javascript:
  - assets/asciinema-player/asciinema-player.min.js
  - javascripts/asciinema-embed.js
